{#
This file is part of EC-CUBE

Copyright(c) LOCKON CO.,LTD. All Rights Reserved.

http://www.lockon.co.jp/

For the full copyright and license information, please view the LICENSE
file that was distributed with this source code.
#}
{% extends '@admin/default_frame.twig' %}

{% set menus = ['shipping', 'shipping_edit'] %}

{% block title %}{{ 'admin.shipping.edit.title'|trans }}{% endblock %}
{% block sub_title %}{{ 'admin.shipping.edit.sub_title'|trans }}{% endblock %}

{% form_theme form '@admin/Form/bootstrap_4_horizontal_layout.html.twig' %}

{% block javascript %}
    <script src="//yubinbango.github.io/yubinbango/yubinbango.js" charset="UTF-8"></script>

    <script>
        // 商品の追加モーダル#検索ボタン
        // 明細の一覧を取得し, モーダル内に描画する
        $('#addProduct').on('click', '#searchProductModalButton', function () {
            var list = $('#searchProductModalList');
            list.children().remove();

            $.ajax({
                url: '{{ url('admin_order_search_product') }}',
                type: 'POST',
                dataType: 'html',
                data: {
                    'id': $('#admin_search_product_id').val(),
                    'category_id': $('#admin_search_product_category_id').val()
                }
            }).done(function(data) {
                $('#searchProductModalList').html(data);
            }).fail(function(data) {
                alert('search product failed.');
            });
        });
        // 商品の追加モーダル#ページャ部分
        // クリックしたページ番号の明細一覧を取得し, モーダル内に描画する
        $('#addProduct').on('click', '.page-link', function (event) {
            event.preventDefault();
            var list = $('#searchProductModalList');
            list.children().remove();
            var url = $(this).attr('href');
            $.ajax({
                url: url,
                type: 'GET',
                dataType: 'html'
            }).done(function(data) {
                $('#searchProductModalList').html(data);
            }).fail(function(data) {
                alert('search product failed.');
            });
        });
        // 商品の追加モーダル#＋ボタン
        // クリックした明細IDを取得し, 親ウインドウに描画する
        $('#addProduct').on('click', '.append-order-item', function (event) {
            var target = event.target;
            // 明細IDを取得
            var id = target.getAttribute('data-order-item-id');
            // 自身の行を削除.
            $(event.target).parents('tr').remove();

            $.ajax({
                type: 'POST',
                dataType: 'html',
                // idで明細情報を取得
                url: '{{ url('admin_shipping_search_item') }}' + '?order-item-id=' + id,
            }).done(function (data) {
                // 親ウインドウに結果を書き出し.
                $("#addOrderItem > tbody").append(data);
            }).fail(function () {
                alert('failed.');
            });
        });
        // 明細行の削除
        $('.addOrderItem').on('click', '.remove-order-item', function (event) {
            event.preventDefault();

            // 自身の行を削除.
            $(event.target).parents('tr').remove();
        });

        {# TODO: Should move logic js into js file intead of twig #}
        var shippedNotifyModal = $('#shippedNotifyModal'),
            shippingForm = $('#form_shipping'),
            shippingDateInput = shippingForm.find('input[name*="shipping_date"]');

        shippingDateInput.on('change', function() {
            if ($(this).val() == '') {
                {% if Shippings.0.isShipped == false %}
                    shippingForm.find('#btn_save')
                        .attr('type', 'submit')
                        .removeAttr('data-toggle')
                        .removeAttr('data-target');
                {%  endif %}
            } else {
                {% if Shippings.0.isShipped == false %}
                    shippingForm.find('#btn_save')
                        .attr('data-toggle', 'modal')
                        .attr('data-target', '#shippedNotifyModal')
                        .attr('type', 'button');
                {% endif %}
            }
        });
        shippingDateInput.trigger('change');
        shippedNotifyModal.on('show.bs.modal', function(e) {
            var viewUrl = $('#viewEmail');
            if (viewUrl.length) {
                $.ajax(viewUrl.data('preview-url')).done(function(res) {
                    viewUrl.find(' > pre').text(res);
                });
            }
        });

    </script>

{% endblock javascript %}

{% block main %}
    <form action="{{ url('admin_shipping_edit', {id: Order.id}) }}"
          method="post" id="form_shipping" class="h-adr">
        <span class="p-country-name" style="display:none;">Japan</span>
        {{ form_widget(form._token) }}
        <div class="modal fade" id="addProduct" tabindex="-1" role="dialog"
             aria-labelledby="addProduct" aria-hidden="true">
            <div class="modal-dialog modal-lg" role="document">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title">{{ 'admin.order.edit.btn.add_product'|trans }}</h5>
                        <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">×</span>
                        </button>
                    </div>
                    <div class="modal-body">
                        {{ form_widget(searchProductModalForm.id, { attr : {'class': 'mb-3',  placeholder : 'admin.order.edit.293' }}) }}
                        {{ form_widget(searchProductModalForm.category_id) }}
                        <button type="button" id="searchProductModalButton"
                                class="btn btn-ec-conversion px-5 mb-4 mt-2">{{ 'admin.order.edit.294'|trans }}</button>
                        <div id="searchProductModalList"></div>
                    </div>
                </div>
            </div>
        </div>
        <div class="c-contentsArea__cols">
            <div class="c-contentsArea__primaryCol">
                <div class="c-primaryCol">
                    {% for shippingField in form.shippings %}
                        <div class="card rounded border-0 mb-4">
                            <div class="modal fade" id="shippedNotifyModal" tabindex="-1" role="dialog"
                                 aria-labelledby="shippedNotifyModal" aria-hidden="true" data-keyboard="false"
                                 data-backdrop="static">
                                <div class="modal-dialog" role="document">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <h5 class="modal-title font-weight-bold">{{ 'admin.shipping.index.829'|trans }}</h5>
                                            <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                                                <span aria-hidden="true">×</span>
                                            </button>
                                        </div>
                                        <div class="modal-body">
                                            <p class="mb-4">{{ 'admin.shipping.index.830'|trans }}</p>
                                            <div id="mail-options">
                                                <div class="font-weight-bold mb-2">{{ 'admin.shipping.index.812'|trans }}</div>
                                                <div class="form-check form-check-inline">
                                                    <label class="form-check-label">
                                                        {{ form_widget(shippingField.notify_email, {'label_attr': {'class': 'checkbox-inline'}}) }}
                                                    </label>
                                                </div>
                                                {% if (attribute(Shippings, loop.index0).id) %}
                                                    <div>
                                                        <div class="d-inline-block" data-toggle="collapse"
                                                             href="#viewEmail" aria-expanded="false"
                                                             aria-controls="viewEmail">
                                                            <a>
                                                                <i class="fa fa-plus-square-o font-weight-bold mr-1"></i>
                                                                <span class="font-weight-bold">{{ 'admin.shipping.index.814'|trans }}</span>
                                                            </a>
                                                        </div>
                                                        <div class="collapse bg-light p-4 ec-collapse bg-ec-formGray"
                                                             id="viewEmail"
                                                             style="word-wrap: break-word; word-break:break-all"
                                                             data-preview-url="{{ url('admin_shipping_preview_notify_mail', {id: attribute(Shippings, loop.index0).id}) }}">
                                                            <pre></pre>
                                                        </div>
                                                    </div>
                                                {% endif %}
                                            </div>
                                        </div>
                                        <div class="modal-footer">
                                            <button class="btn btn-ec-sub" type="button"
                                                    data-dismiss="modal">{{ 'admin.shipping.index.815'|trans }}</button>
                                            <button class="btn btn-ec-conversion"
                                                    type="submit">{{ 'admin.shipping.index.816'|trans }}</button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="card-header">
                                <div class="row">
                                    <div class="col-8">
                                        <div class="d-inline-block" data-toggle="tooltip" data-placement="top"
                                             title="Tooltip">
                                            <span class="card-title">{{ 'admin.shipping.edit.description_title'|trans }}{{ loop.index }}</span>
                                            <i class="fa fa-question-circle fa-lg ml-1" aria-hidden="true"></i>
                                        </div>
                                    </div>
                                    <div class="col-4 text-right">
                                        <a data-toggle="collapse" href="#shipmentOverview" aria-expanded="false"
                                           aria-controls="shipmentOverview">
                                            <i class="fa fa-angle-up fa-lg" aria-hidden="true"></i>
                                        </a>
                                    </div>
                                </div>
                            </div>
                            <div class="collapse show ec-cardCollapse" id="shipmentOverview">
                                <div class="card-body">
                                    <div class="row mb-3">
                                        <div class="col-6">
                                            {# TODO: 納品書出力実装 #}
                                            <button type="button" id="print" class="btn btn-ec-regular">
                                                {{ 'admin.order.edit.label.shipping.print'|trans }}
                                            </button>
                                            {# TODO: 出荷完了メール実装 #}
                                            <button type="button" id="shipped_mail" class="btn btn-ec-regular">
                                                {{ 'admin.order.edit.label.shipping.shipped_mail'|trans }}
                                            </button>
                                        </div>
                                    </div>
                                    <div class="row">
                                        <div class="col-6">
                                            <div class="row form-group">
                                                <label class="col-3 col-form-label">
                                                    {{ 'admin.common.label.name'|trans }}
                                                    <span class="badge badge-primary ml-1">
                                                        {{ 'common.text.message.required'|trans }}
                                                    </span>
                                                </label>
                                                <div class="col">
                                                    <div class="row">
                                                        <div class="col">
                                                            {{ form_widget(shippingField.name.name01, {attr: {placeholder: 'admin.common.label.family_name' }}) }}
                                                            {{ form_errors(shippingField.name.name01) }}
                                                        </div>
                                                        <div class="col">
                                                            {{ form_widget(shippingField.name.name02, {attr: {placeholder: 'admin.common.label.first_name' }}) }}
                                                            {{ form_errors(shippingField.name.name02) }}
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                            <div class="row form-group">
                                                <label class="col-3 col-form-label">
                                                    {{ 'admin.common.label.kana'|trans }}
                                                    <span class="badge badge-primary ml-1">
                                                        {{ 'common.text.message.required'|trans }}
                                                    </span>
                                                </label>
                                                <div class="col">
                                                    <div class="row">
                                                        <div class="col">
                                                            {{ form_widget(shippingField.kana.kana01, {attr: {placeholder: 'admin.common.label.family_name_kana' }}) }}
                                                            {{ form_errors(shippingField.kana.kana01) }}
                                                        </div>
                                                        <div class="col">
                                                            {{ form_widget(shippingField.kana.kana02, {attr: {placeholder: 'admin.common.label.first_name_kana' }}) }}
                                                            {{ form_errors(shippingField.kana.kana02) }}
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                            <div class="row form-group">
                                                <label class="col-3 col-form-label">
                                                    {{ 'admin.common.label.address'|trans }}
                                                    <span class="badge badge-primary ml-1">
                                                        {{ 'common.text.message.required'|trans }}
                                                    </span>
                                                </label>
                                                <div class="col">
                                                    <div class="row mb-3">
                                                        <div class="col form-inline">
                                                            {{ 'admin.order.edit.261'|trans }}
                                                            {{ form_widget(shippingField.postal_code) }}
                                                            {{ form_errors(shippingField.postal_code) }}
                                                        </div>
                                                    </div>
                                                    <div class="row mb-3">
                                                        <div class="col">
                                                            {{ form_widget(shippingField.address.pref) }}
                                                            {{ form_errors(shippingField.address.pref) }}
                                                        </div>
                                                    </div>
                                                    <div class="row mb-3">
                                                        <div class="col">
                                                            {{ form_widget(shippingField.address.addr01, { attr : {placeholder : 'admin.order.edit.262' }}) }}
                                                            {{ form_errors(shippingField.address.addr01) }}
                                                        </div>
                                                    </div>
                                                    <div class="row">
                                                        <div class="col">
                                                            {{ form_widget(shippingField.address.addr02, { attr : { placeholder : 'admin.order.edit.263' }}) }}
                                                            {{ form_errors(shippingField.address.addr02) }}
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                        <div class="col-6">
                                            <div class="row form-group">
                                                <label class="col-3 col-form-label">
                                                    {{ 'admin.common.label.tel'|trans }}
                                                    <span class="badge badge-primary ml-1">
                                                        {{ 'common.text.message.required'|trans }}
                                                    </span>
                                                </label>
                                                <div class="col">
                                                    {{ form_widget(shippingField.phone_number) }}
                                                    {{ form_errors(shippingField.phone_number) }}
                                                </div>
                                            </div>

                                            <div class="row form-group">
                                                <label class="col-3 col-form-label">
                                                    {{ 'admin.common.label.company'|trans }}
                                                </label>
                                                <div class="col">
                                                    {{ form_widget(shippingField.company_name) }}
                                                    {{ form_errors(shippingField.company_name) }}
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-12">
                                        <hr>
                                    </div>
                                    <div class="row">
                                        <div class="col-6">
                                            <div class="row mb-3 form-group">
                                                <label class="col-3 col-form-label">
                                                    {{ 'admin.common.label.tracking_number'|trans }}
                                                </label>
                                                <div class="col">
                                                    {{ form_widget(shippingField.tracking_number) }}
                                                    {{ form_errors(shippingField.tracking_number) }}
                                                </div>
                                            </div>
                                            <div class="row mb-3 form-group">
                                                <label class="col-3 col-form-label">
                                                    {{ 'admin.shipping.edit.delivery_provider'|trans }}
                                                    <span class="badge badge-primary ml-1">{{ 'common.text.message.required'|trans }}
                                                    </span>
                                                </label>
                                                <div class="col">
                                                    {{ form_widget(shippingField.Delivery) }}
                                                    {{ form_errors(shippingField.Delivery) }}
                                                </div>
                                            </div>
                                        </div>
                                        <div class="col-6">
                                            <div class="row mb-3 form-group">
                                                <label class="col-3 col-form-label">
                                                    <i class="fa fa-calendar-check-o fa-fw mr-1" aria-hidden="true"></i>
                                                    {{ 'admin.shipping.edit.date_to_deliver'|trans }}
                                                </label>
                                                <div class="col">
                                                    {{ form_widget(shippingField.shipping_delivery_date) }}
                                                    {{ form_errors(shippingField.shipping_delivery_date) }}
                                                </div>
                                            </div>
                                            <div class="row mb-3 form-group">
                                                <label class="col-3 col-form-label">
                                                    <i class="fa fa-clock-o fa-fw mr-1" aria-hidden="true"></i>
                                                    {{ 'admin.shipping.edit.specified_time_zone'|trans }}
                                                </label>
                                                <div class="col">
                                                    {{ form_widget(shippingField.DeliveryTime) }}
                                                    {{ form_errors(shippingField.DeliveryTime) }}
                                                </div>
                                            </div>
                                            <div class="row mb-3 form-group">
                                                <label class="col-3 col-form-label">
                                                    <i class="fa fa-truck fa-fw mr-1" aria-hidden="true"></i>
                                                    {{ 'admin.shipping.edit.ship_date'|trans }}
                                                </label>
                                                <div class="col">
                                                    {{ form_widget(shippingField.shipping_date) }}
                                                    {{ form_errors(shippingField.shipping_date) }}
                                                </div>
                                            </div>
                                            <div class="row mb-3 form-group">
                                                <label class="col-3 col-form-label">
                                                    <i class="fa fa-refresh fa-fw mr-1" aria-hidden="true"></i>
                                                    {{ 'admin.shipping.edit.last_update_date'|trans }}
                                                </label>
                                                <div class="col">
                                                    <span>{{ attribute(Shippings, loop.index0).id ? attribute(Shippings, loop.index0).update_date|date_min }}</span>
                                                </div>
                                            </div>
                                            <div class="row mb-2 align-items-center">
                                                <label class="col-3 col-form-label">
                                                    <i class="fa fa-user fa-fw mr-1" aria-hidden="true"></i>
                                                    {{ 'admin.shipping.edit.last_modified_by'|trans }}
                                                </label>
                                                <div class="col">
                                                    <span>{{ attribute(Shippings, loop.index0).id ? attribute(Shippings, loop.index0).creator }}</span>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-12">
                                        <hr>
                                    </div>
                                    <div class="row">
                                        <div class="col-12">
                                            <button class="btn btn-ec-regular mr-2" type="button" data-toggle="modal"
                                                    data-target="#addProduct">
                                                {{ 'admin.shipping.edit.add_product'|trans }}
                                            </button>

                                            {# TODO: 処理を確認 #}
                                            <a class="btn btn-ec-regular mr-2" data-toggle="modal" data-target="#addOrderItemType">{{ 'admin.order.edit.btn.add_other_item'|trans }}</a>

                                            {# 明細行のエラー表示 #}
                                            {{ form_errors(shippingField.OrderItemsError) }}

                                        </div>
                                    </div>
                                    <div class="row">
                                        <table id="addOrderItem_{{ loop.index0 }}" class="table table-striped table-sm mb-0 addOrderItem">
                                            <thead class="table-active">
                                            <th class="pt-2 pb-2 pl-3">{{ 'admin.shipping.edit.product_name_and_code'|trans }}</th>
                                            <th class="pt-2 pb-2">{{ 'admin.shipping.edit.price'|trans }}</th>
                                            <th class="pt-2 pb-2">{{ 'admin.shipping.edit.quantity'|trans }}</th>
                                            <th class="pt-2 pb-2">{{ 'admin.shipping.edit.tax_rate'|trans }}</th>
                                            <th class="pt-2 pb-2">{{ 'admin.shipping.edit.sub_total'|trans }}</th>
                                            <th class="pt-2 pb-2 pr-3"></th>
                                            </thead>
                                            <tbody>
                                            {% set OrderItems = attribute(Shippings, loop.index0).OrderItems %}
                                            {% for orderItemField in shippingField.OrderItems %}
                                                {% include '@admin/Shipping/order_item_prototype.twig' with { 'OrderItem': attribute(OrderItems, loop.index0), 'orderItemField': orderItemField } %}
                                            {% endfor %}
                                            </tbody>
                                        </table>
                                    </div>
                                    <div class="col-12">
                                        <hr>
                                    </div>
                                    <div class="row">
                                        <div class="col-12">
                                            <div class="row mb-3 form-group">
                                                <label class="col-3 col-form-label">
                                                    {{ 'admin.shipping.edit.note_title'|trans }}
                                                </label>
                                                <div class="col">
                                                    {{ form_widget(shippingField.note, { attr: { rows: 8 }}) }}
                                                    {{ form_errors(shippingField.note) }}
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                    <div class="card rounded border-0 mb-4">
                        <button type="button" class="btn btn-ec-regular">出荷先を追加</button>
                    </div>
                </div>
            </div>
        </div>

        <div class="c-conversionArea">
            <div class="c-conversionArea__container">
                <div class="row justify-content-between align-items-center">
                    <div class="col-6">
                        <div class="c-conversionArea__leftBlockItem">
                            <a class="c-beseLink" href="{{ url("admin_order_edit", {id: Order.id}) }}">
                                <i class="fa fa-backward" aria-hidden="true"></i>
                                <span>{{ 'admin.order.edit.241'|trans }}</span>
                            </a>
                        </div>
                    </div>
                    <div class="col-6">
                        <div class="row align-items-center justify-content-end">
                            <div class="col-auto">
                                <button type="submit" class="btn btn-ec-conversion px-4" id="btn_save">
                                    {{ 'admin.shipping.edit.register_shipping'|trans }}
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </form>
{% endblock %}
