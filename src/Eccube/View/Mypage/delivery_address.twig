{% extends 'popup.twig' %}

{% set subtitle='新しいお届け先の追加・変更' %}

{% block onload %}
    {% if app.session.flashBag.get('mypage_delivery_address.just_added') %}
        eccube.changeParentUrl('{{ parentPage }}');
        window.close();
    {% endif %}
{% endblock %}

{% block main %}
<div id="window_area">
    <h2 class="title">{{ title }}</h2>
    <p>下記項目にご入力ください。「<span class="attention">※</span>」印は入力必須項目です。<br />
        入力後、一番下の「登録する」ボタンをクリックしてください。</p>

    <form name="form1" id="form1" method="post" action="?">
        <input type="hidden" name="other_deliv_id" value="{{ app.request.get('other_deliv_id') }}" />
        <input type="hidden" name="parent_page" value="{{ parentPage }}" />
        {{ form(form) }}
        <div class="btn_area">
            <ul>
                <li>
                    <a class="btn-action" href="javascript:" onclick="eccube.setValueAndSubmit('form1', 'mode', 'edit'); return false;">
                        <img class="hover_change_image" src="{{ app.config.tpl }}img/button/btn_add_address_complete.jpg" alt="登録する" />
                    </a>
                </li>
            </ul>
        </div>
    </form>
</div>
{% endblock %}
