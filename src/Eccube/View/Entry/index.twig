{% extends 'site_main.twig' %}

{# 装飾のカスタマイズ方法 #}
{% form_theme form _self %}
{% block form_errors %}
{% spaceless %}
    {% if errors|length > 0 %}
    <ul>
        {% for error in errors %}
            <li class="error">{{ error.message }}</li>
        {% endfor %}
    </ul>
    {% endif %}
{% endspaceless %}
{% endblock form_errors %}


{% block main %}

<div id="undercolumn">
    <div id="undercolumn_entry">
        <h2 class="title">{{ title }}</h2>
        <p>
            ご登録されますと、まずは仮会員となります。<br />
            入力されたメールアドレスに、ご連絡が届きますので、本会員になった上でお買い物をお楽しみください。
        </p>
        <form name="form1" id="form1" method="post" action="{{ url('entry_confirm') }}">
            {{ form_widget(form._token) }}
            <table summary="会員登録フォーム">

                <col width="30%" />
                <col width="70%" />
                <tr>
                    <th>お名前<span class="attention">※</span></th>
                    <td>
                        姓&nbsp;{{ form_widget(form.name01) }}&nbsp;
                        名&nbsp;{{ form_widget(form.name02) }}
                        {{ form_errors(form.name01) }}
                        {{ form_errors(form.name02) }}
                    </td>
                </tr>
                <tr>
                    <th>お名前(フリガナ)</th>
                    <td>
                        セイ&nbsp;{{ form_widget(form.kana01) }}&nbsp;
                        メイ&nbsp;{{ form_widget(form.kana02) }}
                        {{ form_errors(form.kana01) }}
                        {{ form_errors(form.kana02) }}
                    </td>
                </tr>
                <tr>
                    <th>メールアドレス<span class="attention">※</span></th>
                    <td>
                        {{ form_widget(form.email) }}<br />
                        {{ form_errors(form.email) }}
                        <span class="attention mini">確認のため2度入力してください。</span>
                    </td>
                </tr>
                <tr>
                    <th>希望するパスワード<span class="attention">※</span><br />
                    </th>
                    <td>
                        {{ form_widget(form.password.first, {type: "password"}) }}<br />
                        {{ form_widget(form.password.second, {type: "password"}) }}
                        {{ form_errors(form.password.first) }}
                        <p><span class="attention mini">半角英数字8〜16文字でお願いします。（記号可）</span></p>
                        <p><span class="attention mini">確認のために2度入力してください。</span></p>
                    </td>
                </tr>
            </table>

            <div class="btn_area">
                <ul>
                    <li>
                        <input type="image" class="hover_change_image" src="{{ constant('Eccube\\Constant::TPL')}}img/button/btn_confirm.jpg" alt="確認ページへ" name="confirm" value="1" id="confirm" />
                    </li>
                </ul>
            </div>
        </form>
    </div>
</div>
{% endblock %}