{% if blocs|length > 0 %}
        {% for bloc in blocs %}
            <!-- ▼{{ bloc.name }} -->
            {% if bloc.phpPath %}
                {{ render(path('bloc_' ~ bloc.filename)) }}
            {% else %}
            	{{ include('frontparts/bloc/' ~ bloc.tplPath) }}
            {% endif %}
            <!-- ▲{{ bloc.name }} -->
        {% endfor %}
{% endif %}